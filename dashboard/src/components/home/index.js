import React from 'react';
import './style.css';
import Table from './table';

const Home = () => (
  <div style={{width: '100%', overflowX: 'hidden'}}>
    <h1>Home</h1>
    <Table />
  </div>
);

export default Home;