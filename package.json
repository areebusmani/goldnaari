{
  "name": "goldnaari",
  "version": "5.0.0",
  "dependencies": {
    "body-parser": "*",
    "dotenv": "^16.0.3",
    "express": "*",
    "method-override": "*",
    "morgan": "*",
    "pug": "*",
    "serve-favicon": "*",
    "stylus": "*"
  },
  "engines": {
    "node": "*",
    "npm": "*"
  },
  "scripts": {
    "start": "node app.js",
    "heroku-postbuild": "cd client && npm install --only=dev && npm install && npm run build"
  }
}
